/* ============================================
   MAIN.JS — Quiz App Entry Point
   ============================================
   Purpose: Initializes the Quiz Application.
   
   This file is the starting point. It runs when
   the page loads and sets up the quiz.
   ============================================ */

// Run when page loads — show the start screen
document.addEventListener("DOMContentLoaded", function () {
  // Show the start screen (defined in ui.js)
  renderStartScreen();
});
